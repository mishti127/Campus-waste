# Campus Waste - Dashboard

A simple responsive HTML/CSS/JS dashboard inspired by the screenshot you provided.

Files:
- `index.html` - the page
- `styles.css` - styles
- `script.js` - simple click handlers

How to run:
1. Open `index.html` in your browser (double-click or use Live Server extension).

Next steps you might want:
- Replace the placeholder alerts with real routes or modals.
- Hook `Scan Waste` to a camera/webcam + ML model or API.
- Connect `Find Nearest Bin` to a map provider (Leaflet/Google Maps).

